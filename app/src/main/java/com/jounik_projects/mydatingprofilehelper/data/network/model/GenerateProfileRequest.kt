package com.jounik_projects.mydatingprofilehelper.data.network.model

/**
 * Класс данных для запроса генерации анкеты на бэкенде.
 * Включает свойства, необходимые для вызова API генерации, если таковые требуются в теле запроса.
 */
data class GenerateProfileRequest(
    // Здесь могут быть дополнительные свойства, если бэкенд ожидает их в теле запроса.
    // Например, параметры для настройки генерации, если такие будут предусмотрены.
    // Пока оставляем пустым, так как UserId будет получен бэкендом из JWT.
)